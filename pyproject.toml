[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "url"
version = "1.0.0"
description = "URL validator, expander, and parser"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"

[project.scripts]
url = "url:main"
